// Shared framing for the two legal pages.
//
// The Terms and Privacy pages ship as structured, readable templates so the
// public shell is complete and every footer link resolves. They are NOT legal
// advice and are not fit to be relied on: the banner below says so at the top
// of both pages, and Prompt 11 §4.7 requires it to stay there until counsel
// supplies the real text.

export const LEGAL_TEMPLATE_NOTICE =
  'Template for demonstration — replace with counsel-reviewed text before launch.'

/** ISO date shown as "Last updated" on both legal pages. */
export const LEGAL_LAST_UPDATED = '2026-08-01'
